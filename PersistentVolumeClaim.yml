apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc
  namespace: dash2
spec:
  storageClassName: ""
  accessModes:
    - ReadWriteOnce 
  volumeName: mongo-data
  resources:
    requests:
      storage: 10Gi
