kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: dash-pvc
  namespace: dash
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 15Gi
